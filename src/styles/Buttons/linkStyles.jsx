import React from "react";

const linkStyles = {
    direction: "row",
    alignItems: "center",
    spacing: 0.2,
    cursor: "pointer",
    color: "text.primary",
    "&:hover": {
      color: "text.secondary",
    },
    whiteSpace: "nowrap",
  };

export default linkStyles;